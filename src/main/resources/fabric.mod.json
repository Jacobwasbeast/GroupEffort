{
  "schemaVersion": 1,
  "id": "groupeffort",
  "version": "${version}",
  "name": "GroupEffort",
  "description": "Prevents players from playing on the server until the server reaches the quota needed.",
  "authors": [],
  "contact": {},
  "license": "MIT",
  "icon": "assets/groupeffort/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "net.jacobwasbeast.groupeffort.GroupEffort"
    ],
    "client": [
      "net.jacobwasbeast.groupeffort.client.GroupEffortClient"
    ]
  },
  "mixins": [
    "groupeffort.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
